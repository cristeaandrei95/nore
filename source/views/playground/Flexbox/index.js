import React, { Component } from "react";
import $ from "./style.css";

export default () => (
	<b class={$.page}>
		<b class={$.container}>
			<b class={$.item}>two words</b>
			<b class={$.item}>three words now</b>
			<b class={$.item}>
				lorem ipsum sit dolor valor rem et mid das vaulus pino noir
			</b>
			<b class={$.item} />
			<b class={$.item} />
			<b class={$.item} />
			<b class={$.item} />
			<b class={$.item} />
			<b class={$.item} />
		</b>
	</b>
);
