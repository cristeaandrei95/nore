import React, { Component } from "react";
import $ from "./style.css";

export default () => (
	<b class={$.section}>
		<b class={$.tag}>one</b>
		<b class={$.tag}>two</b>
		<b class={$.tag}>three</b>
	</b>
);
