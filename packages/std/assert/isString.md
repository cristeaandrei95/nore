---
title: isString
collection: assert
tags:
  - assert
  - validate
  - string
---

Returns `true` if the _source_ is a string.

```js
import { isString } from "@nore/std/assert";

isString("string");
// => true
```
