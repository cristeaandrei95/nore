---
title: isBoolean
collection: assert
tags:
  - assert
  - validate
  - boolean
---

Returns `true` if the _source_ is `true` or `false`.

```js
import { isBoolean } from "@nore/std/assert";

isBoolean(true);
// => true
```
