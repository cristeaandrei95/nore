---
title: isFunction
collection: assert
tags:
  - assert
  - validate
  - function
---

Returns `true` if the _source_ is an function.

```js
import { isFunction } from "@nore/std/assert";

isFunction(() => {});
// => true
```
