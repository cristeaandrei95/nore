---
title: shuffle
collection: array
tags:
  - array
  - shuffle
---

Returns a new array with the items shuffled.

`shuffle(list)`

- `list` — the target array

```js
import { shuffle } from "@nore/std/array";

shuffle(["a", "b", "c", "d"]);
// => ["b", "d", "c", "a"]
```
