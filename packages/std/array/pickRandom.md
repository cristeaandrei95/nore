---
title: pickRandom
collection: array
tags:
  - array
  - pickRandom
---

Pick a random item from an array.

`pickRandom(list)`

- `list` — the target array

```js
import { pickRandom } from "@nore/std/array";

pickRandom(["a", "b", "e"]);
// => "b"
```
